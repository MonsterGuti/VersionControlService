# VersionControlService
Custom version control and document management system with file tracking, commits, and version history. Supports user authentication with roles and permissions. Users can create documents, commit changes, review (approve/reject) versions, view active versions, and compare changes via diff.
